apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: reader-monitor
  namespace: default
spec:
  selector:
    matchLabels:
      app: reader-app
  endpoints:
  - port: http-web
    path: /prometheus
  targetLabels:
    - application
